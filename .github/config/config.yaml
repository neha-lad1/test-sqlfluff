{
  "release_version": "${{ github.event.inputs.release_version }}",
  "projects": [
    {
      "name": "WATER_DM",
      "paths": {
        "internal": "ADW/${{ github.event.inputs.release_version }}/WATER_DM/tables/internal",
        "external": "ADW/${{ github.event.inputs.release_version }}/WATER_DM/tables/external"
      }
    },
    {
      "name": "COMMON_DM",
      "paths": {
        "internal": "ADW/${{ github.event.inputs.release_version }}/COMMON_DM/tables/internal",
        "external": "ADW/${{ github.event.inputs.release_version }}/COMMON_DM/tables/external"
      }
    }
    // Add additional projects similarly
  ]
}
